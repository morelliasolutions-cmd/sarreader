# Configuration du serveur
PORT=5001
SAR_EXTRACTION_HOST=0.0.0.0
SAR_EXTRACTION_DEBUG=False

# CORS - origines autorisées (séparées par virgule)
# Utiliser * pour autoriser toutes les origines (dev uniquement)
ALLOWED_ORIGINS=https://agtelecom.connectfiber.ch,http://localhost:3000

# Taille maximum des uploads (en MB)
MAX_UPLOAD_SIZE_MB=50

# Timeout d'extraction (en secondes)
EXTRACTION_TIMEOUT_SECONDS=60

# Niveau de log (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s
